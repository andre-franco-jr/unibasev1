name: unienergy_app
description: App de monitoramento de usinas solares Unienergy
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1
  
  # Networking
  dio: ^5.4.0
  
  # Local storage
  shared_preferences: ^2.2.2
  
  # File management
  path_provider: ^2.1.1
  
  # Charts
  fl_chart: ^1.1.1
  
  # Icons
  cupertino_icons: ^1.0.6
  
  # Internacionalização
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true  
  assets:
    - assets/images/
    - assets/fonts/
  
  fonts:
    - family: Viga
      fonts:
        - asset: assets/fonts/Viga-Regular.ttf
    
    - family: Montserrat
      fonts:
        - asset: assets/fonts/montserrat.regular.ttf